{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allows the main Relevect window to use core IPC and the native folder picker command.",
  "windows": ["main"],
  "permissions": ["core:default", "core:app:default", "allow-pick-folder"]
}
